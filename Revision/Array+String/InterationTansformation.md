
# Note:- .forEach() - chainable nhi hota kyu ki it return the undefined

1. If we want ki like hr elment ko transform krna ha to -> .map() -> newArray return krega 
2. agr hr element pr bs loop lgana hai - .forEach() -> undefined
3. agr apne ko kuch delete/remove krna hot hai to - .filter() -> if condition matched - new Array return for that 
4. single Value of any type - .reduce()


