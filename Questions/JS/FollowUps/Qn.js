// Debounce vs Throttle (real use-case)

// Closure — real bug example

// this binding (normal vs arrow)

// call / bind / apply — when bind needed

// Shallow vs Deep copy — real bug story

// Event bubbling vs capturing — preventDefault vs stopPropagation

// Promise order output

// == vs === (coercion example)

// Reference vs value (array/object)

// Hoisting (function vs var vs let)

// Polyfill logic explanation (map/reduce)

// Error handling in async JS


// Object.entries(obj) - key-value mein change kr deta hai [[k,v]] obj ko
// though wich ab array bn gya to for o lha ke tracerse kr lo